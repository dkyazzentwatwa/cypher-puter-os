# ESP32 Bit Pirate

## Cypher OS Package

| Field | Value |
| --- | --- |
| Catalog slug | `esp32-bit-pirate` |
| SD binary | `/cypher-puter/apps/esp32-bit-pirate.bin` |
| Source repo | https://github.com/dkyazzentwatwa/cardputer-app-bundle |
| Local source path | `/Users/cypher/Documents/GitHub/new-cardputer-apps/ESP32-Bit-Pirate` |
| Build profile | `cardputer-adv` |
| Extra SD paths | None required |
| Return path | Type `launcher`, `return`, `cypher`, or `home` at the prompt, or press `Fn+Del` in standalone mode. |
| Package note | Built from the public `cardputer-app-bundle` repo while the default local checkout remains `../new-cardputer-apps`. |

## Overview

ESP32 Bit Pirate is a Cardputer ADV hardware bus and protocol toolkit. Cypher OS
packages the sketch app binary into the SD catalog without changing the
launcher release asset model.

## Return To Cypher OS

Use one of the typed return aliases at the terminal prompt, or press `Fn+Del`
from standalone keyboard input.
